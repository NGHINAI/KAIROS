# codex app-server protocol bindings (VENDORED — do not hand-edit)

Generated by `codex app-server generate-ts` from a PINNED codex.
- Pinned version: `codex-cli 0.133.0`
- Regenerate (on a version bump ONLY): `codex app-server generate-ts --out src/daemon/codex/proto`
- A version bump is a breaking-change test pass (re-run the contract fixture, ref docs/clicky-migration/02 + 14§C14).

## A0 validation (2026-06-13) — architecture claims confirmed against these bindings
- TurnStartParams: NO `instructions` field; HAS per-turn `effort: ReasoningEffort` + `model` overrides ✓ (v2/TurnStartParams.ts)
- thread/inject_items EXISTS: `{threadId, items: JsonValue[]}` = our per-turn delta channel ✓ (v2/ThreadInjectItemsParams.ts)
- ThreadStartParams HAS `baseInstructions` (durable persona) + native `personality` + `config` + `ephemeral` ✓
- ReasoningEffort = none|minimal|low|medium|high|xhigh ✓
- ThreadItem union = the exact ledger-translation shapes (mcpToolCall{server,tool,status,result,error,durationMs}, agentMessage, reasoning, commandExecution, …)
- NATIVE capabilities discovered: `Personality`, `ThreadMemoryMode`/`MemoryCitation` (codex has its own memory), `collabAgentToolCall` (native sub-agents w/ model+effort). See docs/clicky-migration/15-protocol-findings.md.
